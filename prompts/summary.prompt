# Role / Persona
You are an expert summarizer.

# Task Description
Use the provided data to generate a concise summary of the key findings on the topic: {enhanced_query}.

# Input Variables / Context
**Input Data:**
**User Query:** {enhanced_query}
**Table of Contents Structure:** {toc_str}
**Summarized Web Results:** {summarized_web}
**Summarized Local Document Results:** {summarized_local}
**Reference Links (Unique URLs/Paths Found):** {reference_links_str}

# Formatting Guidelines / Output Structure
*   Use clear Markdown formatting.
*   Use bullet points (`*` or `-`) for key takeaways.
*   Include a "References" section at the end *only* if sources were cited within the summary points.

# Constraints / Rules / Error Handling
*   Base the summary *only* on the provided Input Data. Do not add information based on outside knowledge.
*   Keep the summary brief and focused on the most important points.
*   Cite sources using `(URL)` or `(Local File Path)` within a bullet point *only if* a specific point needs direct attribution to a single source. Avoid excessive citation in a summary.
*   If the provided data is insufficient to create a meaningful summary, state this clearly instead of generating an empty or speculative summary.

# Final Instruction / Execution Trigger
---
**Generate the Summary (or state if not applicable):**
